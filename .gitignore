# ── Python 기본 ───────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class

# ── 가상환경 ─────────────────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/
pip-wheel-metadata/
*.egg-info/
.eggs/
pip-log.txt
pip-delete-this-directory.txt

# ── 빌드/배포 산출물 ─────────────────────────────────────────────────────────
build/
dist/
wheels/
*.egg
*.manifest
*.spec

# ── Flask 전용 ───────────────────────────────────────────────────────────────
# 앱 비밀설정/런타임 파일 두는 곳 (개발 디폴트)
instance/
# 개발용 SQLite DB 무시 (원하면 주석 처리해서 커밋해도 됨)
*.db
*.sqlite
*.sqlite3
# Flask 실행 환경파일
.flaskenv

# ── 환경변수 파일 (python-dotenv 등) ─────────────────────────────────────────
.env
.env.*
!.env.example   # 예시 템플릿은 버전에 포함

# ── 에셋/캐시 ────────────────────────────────────────────────────────────────
.webassets-cache
.cache
.pytest_cache/
htmlcov/
coverage.xml
.coverage
.coverage.*

# ── 정적 분석/타입체커 ───────────────────────────────────────────────────────
.mypy_cache/
.dmypy.json
.pyre/
.pytype/
.ruff_cache/
pylint-report.txt

# ── IDE / 편집기 ────────────────────────────────────────────────────────────
.vscode/
.idea/
*.iml

# ── OS 잡파일 ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Docker / 프로세스 파일 ──────────────────────────────────────────────────
docker-compose.override.yml
*.pid
celerybeat-schedule
celerybeat.pid

# ── 주피터 노트북 ───────────────────────────────────────────────────────────
.ipynb_checkpoints

# ── Node(프론트 빌드 쓴다면) ────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-error.log*

# ── Sphinx 문서 ─────────────────────────────────────────────────────────────
docs/_build/

# ── Alembic (Flask-Migrate) ─────────────────────────────────────────────────
# 보통 migrations/는 버전에 포함합니다. 굳이 무시하려면 아래 주석 해제
# migrations/

# ── 로그 ────────────────────────────────────────────────────────────────────
logs/
*.log
